# Chat 'n' Hook Documentation repo
This repo contains all documentation for chat 'n' hook

## Installation
Run `pip install -r requirements.txt`

## Building docs
Run `make html` to build the docs.
The output will be under `build/html`